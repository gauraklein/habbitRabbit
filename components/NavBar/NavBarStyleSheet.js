import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    navBar: {
        flex: 1,
        flexDirection: 'row',
        // backgroundColor: '#ddd',
        alignItems: 'center',
        justifyContent: 'center',
        color: '#ddd',
       
        // overflow: "hidden"
    },
    text: {
        color: 'black'
    }

});
